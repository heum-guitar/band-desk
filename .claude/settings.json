{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"/Users/hmc/Desktop/코덱스 폴더/Band Desk\" && git add -A && git diff --cached --quiet || (git commit -m \"Auto-save: $(date '+%Y-%m-%d %H:%M')\" && git push origin main) 2>/dev/null || true",
            "timeout": 30,
            "async": true,
            "statusMessage": "GitHub에 자동 저장 중..."
          }
        ]
      }
    ]
  }
}
